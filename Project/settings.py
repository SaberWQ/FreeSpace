# -*- coding: utf-8 -*-
"""
Налаштування проекту - основний конфігураційний файл
Містить всі параметри для роботи додатку
"""

# Імпорт модуля для роботи з операційною системою
import os

# Клас з налаштуваннями проекту
class Config:
    """
    Основний клас конфігурації проекту
    Містить всі налаштування для Flask додатку
    """
    
    # Секретний ключ для захисту сесій та cookies
    # Береться з змінних оточення або використовується значення за замовчуванням
    SECRET_KEY = os.environ.get('SECRET_KEY') or 'exoplanet-habitability-secret-key'
    
    # URL API NASA Exoplanet Archive для отримання даних про екзопланети
    EXOPLANET_API_URL = 'https://exoplanetarchive.ipac.caltech.edu/TAP/sync'
    
    # Час життя кешу в секундах (3600 секунд = 1 година)
    CACHE_TIMEOUT = 3600
    
    # Шлях до файлу з кешованими даними про планети
    DATA_CACHE_FILE = 'data/exoplanets_cache.csv'
    
    # Ваги для розрахунку індексу придатності до життя
    # Кожен параметр має свою вагу від 0 до 1, сума всіх ваг = 1.0
    HABITABILITY_WEIGHTS = {
        'radius': 0.20,          # Вага радіусу планети (20%)
        'mass': 0.15,            # Вага маси планети (15%)
        'temperature': 0.25,     # Вага температури (25%) - найважливіший параметр
        'stellar_flux': 0.15,    # Вага світлового потоку від зірки (15%)
        'orbital_period': 0.10,  # Вага орбітального періоду (10%)
        'eccentricity': 0.10,    # Вага ексцентриситету орбіти (10%)
        'distance': 0.05         # Вага відстані від Землі (5%)
    }
    
    # Оптимальні діапазони параметрів для підтримки життя
    # Базуються на параметрах Землі як еталону
    OPTIMAL_RANGES = {
        # Радіус планети (в радіусах Землі)
        'radius': {
            'optimal': 1.0,  # Оптимальне значення - як у Землі
            'min': 0.5,      # Мінімальне прийнятне значення
            'max': 2.5       # Максимальне прийнятне значення
        },
        # Маса планети (в масах Землі)
        'mass': {
            'optimal': 1.0,  # Оптимальне значення - як у Землі
            'min': 0.3,      # Мінімальне прийнятне значення
            'max': 3.0       # Максимальне прийнятне значення
        },
        # Температура поверхні (в Кельвінах)
        'temperature': {
            'optimal': 288,  # Оптимальне значення - середня температура Землі (15°C)
            'min': 200,      # Мінімальне прийнятне значення (-73°C)
            'max': 350       # Максимальне прийнятне значення (77°C)
        },
        # Світловий потік від зірки (відносно Землі)
        'stellar_flux': {
            'optimal': 1.0,  # Оптимальне значення - як у Землі
            'min': 0.5,      # Мінімальне прийнятне значення
            'max': 1.5       # Максимальне прийнятне значення
        },
        # Орбітальний період (в днях)
        'orbital_period': {
            'optimal': 365,  # Оптимальне значення - як у Землі (1 рік)
            'min': 100,      # Мінімальне прийнятне значення
            'max': 700       # Максимальне прийнятне значення
        },
        # Ексцентриситет орбіти (0 = коло, >0 = еліпс)
        'eccentricity': {
            'optimal': 0.0,  # Оптимальне значення - кругова орбіта
            'min': 0.0,      # Мінімальне прийнятне значення
            'max': 0.5       # Максимальне прийнятне значення
        },
        # Відстань від Землі (в парсеках)
        'distance': {
            'optimal': 0,    # Оптимальне значення - чим ближче, тим краще
            'min': 0,        # Мінімальне прийнятне значення
            'max': 1000      # Максимальне прийнятне значення
        }
    }
